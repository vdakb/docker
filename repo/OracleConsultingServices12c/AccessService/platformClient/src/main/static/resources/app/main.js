define([bootstrap]
, function (require) {
    // load any app-specific modules with a relative require call
    //var messages = require('./msg/resource');
//    var captcha  = require('./captcha/request')
//    var config   = require('./captcha/config')
  }
);