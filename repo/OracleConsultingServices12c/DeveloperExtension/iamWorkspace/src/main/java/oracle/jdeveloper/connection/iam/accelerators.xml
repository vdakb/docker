<context scope="global">
  <preset name="Classic">
    <map action="mds.action.navigator">
      <accel>shift control M</accel>
    </map>
    <map action="ods.action.navigator">
      <accel>shift control D</accel>
    </map>
  </preset>
  <preset name="Default">
    <map action="mds.action.navigator">
      <accel>shift control M</accel>
    </map>
    <map action="ods.action.navigator">
      <accel>shift control D</accel>
    </map>
  </preset>
  <preset name="Default MacOS X">
    <map action="mds.action.navigator">
      <accel>control meta M</accel>
    </map>
    <map action="ods.action.navigator">
      <accel>control meta D</accel>
    </map>
  </preset>
  <preset name="Emacs">
    <map action="mds.action.navigator">
      <accel>shift control M</accel>
    </map>
    <map action="ods.action.navigator">
      <accel>shift control D</accel>
    </map>
  </preset>
  <preset name="Visual C++">
    <map action="mds.action.navigator">
      <accel>shift control M</accel>
    </map>
    <map action="ods.action.navigator">
      <accel>shift control D</accel>
    </map>
  </preset>
  <preset name="Eclipse">
    <map action="mds.action.navigator">
      <accel>shift control M</accel>
    </map>
    <map action="ods.action.navigator">
      <accel>shift control D</accel>
    </map>
  </preset>
</context>